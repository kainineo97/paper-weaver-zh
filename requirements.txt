# ReoNa-paper-digest dependencies (Python 3.10+)
# Install: pip install -r requirements.txt
# NOTE: keep this file ASCII-only; Chinese comments break pip on
# Windows systems with GBK default encoding (UnicodeDecodeError).

# Core rendering
css-inline>=0.14
markdown>=3.5
pymdown-extensions>=10.0   # pymdownx.* extensions used by render.py
emoji>=2.8

# Browser automation (save draft to WeChat)
playwright>=1.40

# Config & data
PyYAML>=6.0

# PDF figure extraction (pdf-figure.py)
PyMuPDF>=1.23

# Cover generation (cover-gen.py)
Pillow>=10.0
openai>=1.40

# Tests
pytest>=7.0
