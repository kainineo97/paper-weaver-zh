# 分支包验证记录

日期：2026-09-08。版本：paper-weaver-zh 0.1.1-fork。环境：Windows、Python 3.12、现有 Python 依赖与本地无头 Chromium。

## 本次实际完成

| 项目 | 结果 |
|---|---|
| paper-weaver 单元测试 | 34/34 通过：原 29 项与新增 5 项仓库依赖解析测试 |
| ReoNa 发布逻辑检查 | 独立运行 `tests/test_publish_logic.py`，15/15 通过；未连接微信后台 |
| 科研措辞 lint 检查 | 独立运行 `tests/test_editorial_lint.py`，8/8 通过 |
| skill 结构验证 | paper-weaver-zh 与本次调整路径说明的 reona-paper-digest-zh 均通过 quick_validate |
| doctor | 五项依赖均从当前仓库解析，关键文件指纹匹配；六项 Python 模块可用 |
| 不依赖全局安装 | 合成临时仓库配合不存在的配置根，五项依赖均正确解析；另测显式覆盖、平铺安装兼容、假仓库拒绝与根目录去重 |
| 实际渲染链 | 合成短稿经过 check、publish-check、ReoNa render 和双页同步，成功完成本地 build |
| 页面检查 | reading / preview 各在 900 px 与 390 px 检查：无横向溢出，测试图片加载完成，原生上标存在，四次读取的正文 DOM 内容相同 |
| 页面目视检查 | 已查看桌面和手机预览截图，未见裁切或坏图；本次图片为极小的上游测试夹具，不用于评价科研插图清晰度 |
| 流程图复用 | HTML 与此前通过 Archify 校验和浏览器检查的产物逐字节相同，未重新绘图或修改运行时 |
| 源码与资料分离 | ZIP 仅包含仓库文件；合成渲染产物、打包脚本、验证环境、真实论文与用户对话不入包 |

根目录两个历史测试脚本使用自带的检查入口，不是 unittest TestCase。曾尝试通用 unittest discovery，虽触发其输出，但因零 TestCase 返回非零；已改用上述独立命令并确认退出码均为 0，README 同步使用正确命令。

## 已知边界

- 本次渲染稿明确标为合成测试，不包含真实科学结论；批准标记只属于测试夹具，不冒充科学审核。
- 短稿和无封面警告符合测试设计。无封面时，上游复制提示仍写“包含封面”，实际页面没有空白封面或坏图；本次未改动该提示文案。
- 字体栈保留微软雅黑优先配置，实际字体取决于运行机器的字体安装情况。
- 未重新执行多论文完整长文写作、ImageGen、真实微信粘贴/保存、在线部署或远端 Git 操作。
- 之前 0.1.0-local 的真实单论文短篇试跑只作为历史证据，不称为本次重新完成。
- 第三方 article-writing / note-organizing 保留原始前置元数据（包含其自有 version/author 字段），未为了本机校验器而改写上游 skill。组合层按路径读取它们；各产品的独立安装器兼容性需另测。
- 常见密钥标记、开发机绝对路径和私人对话文件名经过静态扫描，未发现匹配；这不是完整安全审计。

## 流程图证据

类型 workflow，图源 SHA-256：`ebc4659bc899cdb2c6f522d4b6a142dcb4fa0b3cdf3e0836154211b989c743e6`。

HTML SHA-256：`bf22b11b82253598cfb00b28996f0b07ecc39fad2e07ef671a0e97e17cd147a0`。

此前确定性校验为 9/9 showcase、0 错误、0 警告；自动浏览器证据 passed，1440×900、1600×1000、1920×1080、2048×1320 无页面溢出，并已目视检查最小和最大尺寸的深浅主题截图。视觉判断只覆盖默认 READ / Still 构图，不声称所有交互都逐项验证。最小视口的辅助文字较小，细读可使用缩放。

## 打包完整性

`BUNDLE-MANIFEST.json` 列出包内其他文件的字节数、SHA-256 和来源，便于上传前后逐项比较。ZIP 根目录直接包含 README、scripts、skills 等，不多套目录；没有 `.git`、`.env`、登录态、缓存或临时测试目录。最终 ZIP 数量、体积及 SHA-256 同时写入包外的交付记录，避免压缩包摘要的自引用。
